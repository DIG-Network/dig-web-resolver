// Build-time constant injected by esbuild / vitest `define` — the package semver,
// stamped into the coexistence sentinel so a page can see which build claimed it.
declare const __DWR_VERSION__: string;
